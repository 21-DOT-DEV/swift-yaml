# Plan — Phase 3 · F4: Insert a missing scalar (`YAMLEditor.insert`)

| | |
|---|---|
| **Feature** | Roadmap **Phase 3 — In-Place & Comment-Preserving Editing** ([Roadmap/phase-3-in-place-editing.md](../../Roadmap/phase-3-in-place-editing.md)), a new **feature 4** (a natural add, not previously roadmapped). A new `YAMLEditor.insert(_ yaml:, at:, to:)` that writes a scalar **that isn't there yet** — it (a) **fills** a value that is currently YAML null (a key with nothing after it, `~`, or `null`), and (b) **adds** a brand-new key when its surrounding block already exists, appending one line at the siblings' indentation. **Add-only:** it refuses (typed error) when a real value is already present — that stays the change command's (`set`) job. Same byte-splice, comment-preserving mechanic as `set`/`unset` (no re-emit). Motivation: a downstream command that must add or fill a field can do it in place instead of a full-document rewrite (which loses the byte-preserving win for that write). |
| **Status** | **IMPLEMENTED — merged to `main`** (PR #7, 2026-07-16). Scope + design via the cold-reader Q&A (five unknowns) + a pre-implementation Q&A (four mechanical unknowns) + four empirical probes (§7). `insert` + `valueAlreadyPresent` + `InsertTests` (19) + `EngineNullContract` bump-tripwires (3); green in three passes. This was the fourth Phase 3 feature (its first three had shipped); with it landed, all four are `IMPLEMENTED` and the phase's **ACTIVE → COMPLETE** call is pending. |
| **Decisions** (this session) | **D1 — Scope (Q1 = B).** Two cases: **fill** a null value, and **insert** a brand-new leaf key whose parent block **already exists** (append one line). *Out:* creating missing parent blocks ("`mkdir -p`"), and appending to a list — both deferred. **D2 — API shape (Q2 = A).** A **new, separately-named command** (`insert`), not a switch on `set` and not a relaxation of `set`'s contract. Grounded: the Swift API guidelines' "clarity at the call site" and the boolean-flag anti-pattern rule out a `set(…, creatingIfMissing:)` switch; Foundation *adds* a named method (`updateValue`) rather than mutating an existing setter, ruling out silently changing `set`. **D3 — Placement (Q3 = A).** A new key lands **at the end of its block**, copying its siblings' indentation. Grounded: the comparable comment-preserving editor ([ruamel.yaml](https://ruyaml.readthedocs.io/en/latest/example.html)) defaults to append-at-end; there is no ecosystem norm for alphabetical order (yamllint's key-ordering rule is **off by default**); append-at-end preserves author order and yields a one-line diff. **D4 — Already-present (Q4 = A).** **Add-only**: if the target already holds a real value, **throw a typed error** — never overwrite. Grounded: Swift's standard library splits this exact fork into two named methods — `Set.insert` (add-only, won't replace) vs `Set.update(with:)` (upsert); our `set` is the "update" half, so `insert` is the add-only half. "Fail loud / least astonishment" says don't silently clobber; the throw (vs a silent no-op) matches how `set`/`unset` already report out-of-domain cases. **D5 — What counts as blank (Q5 = A).** Fill YAML's three genuine no-value spellings (bare `key:`, `~`, `null`/`Null`/`NULL`); an **empty string** (`""`/`''`) is a real value → refused (defer to `set`). **Detect blank from the parser's node kind** (null vs scalar): a probe confirmed our vendored yaml-cpp 0.6.2 labels the no-value spellings *null* and `""`/`''` *scalar*, cleanly — so the label is trusted, and pinned by **bump-tripwire tests** (§4). *(Correction: the earlier note cited [yaml-cpp #1290](https://github.com/jbeder/yaml-cpp/issues/1290) as an empty-string-becomes-null risk. It is the reverse — a **0.8.0** bug where a null **read out as text** returns `"null"`, on the **decode** path, not classification. It doesn't affect our 0.6.2 or `insert`'s labeling; a defensive source-byte check would guard the wrong direction and be un-runnable dead code. Its real exposure is a future engine bump on the decode path — the tripwires catch it.)* |
| **Assumptions** (in force) | **A1** API mirrors `set`: `public static func insert(_ yaml: String, at path: [YAMLPath.Component], to value: Value) throws -> String` on `YAMLEditor`, reusing `YAMLPath.Component`, `YAMLEditor.Value`, `YAMLEditError`, and `YAMLSerialization.emitScalarToken`. Name `insert` mirrors `Set.insert`; **open to `add` at review** (the `to:` label is likewise reviewable). **A2** Byte-splice, no re-emit; comments, blank lines, key order, indentation, and quoting **elsewhere** stay byte-identical. Reuses the guarded parse, anchor/alias-document refusal, and validated path-walk from `set`/`unset`'s shared machinery. **A3** **Block-style parent maps only.** A parent written inline (`{…}`) or a list/sequence target is **refused** with a typed error (as `set`/`unset` refuse inline collections), never mis-edited. **A4** A **new error case** `valueAlreadyPresent(path:)` for D4; the rest reuse existing cases (a parent that **doesn't resolve** → `pathNotFound`; a parent that resolves but is **unusable** — inline `{…}` map, list, or non-map — and inline/collection value shapes → `unsupportedValueShape`; malformed → `malformedDocument`; reference-using doc → `documentUsesAnchorsOrAliases`). Final taxonomy pinned in §3. **A5** Single document. **A6** Tests from the YAML 1.2.2 examples with the re-parse **oracle** (the result parses; the path now holds the intended value; only the filled span / one added line changed), plus the debug / release-whole-module / downstream-`InteropRepro` three passes. Left uncommitted for review. **A7** Roadmap: a new **Phase 3 feature 4** entry (`PLANNED` now → `IMPLEMENTED` at merge) + a `Specs/README.md` row; Phase 3 stays `ACTIVE`. No `Package.swift`/shim/vendored changes (reuses the shipped Mark/span shim). |

## 1. Goal & success criteria

Let a caller give a path a scalar value that **isn't there yet** — filling a null, or adding a new key line — with the only textual change being the filled span or the one added line; everything else byte-identical; the result re-parses to the intended value. Add-only: a path that already holds a real value returns a typed error, never a silent overwrite.

**Concrete criteria:**
- `insert("a:", at: ["a"], to: 1)` → `a: 1`; likewise `a: ~` and `a: null` → `a: 1`. Re-parses to `1`.
- `insert(block, at: ["server", "tls"], to: true)` where `server` is an existing block map without `tls` → a new `tls: true` line appended after the last `server` child, at the children's indentation; siblings and comments byte-identical; re-parses.
- Target already holds a real value → `valueAlreadyPresent` (use `set` to change it).
- Target is an empty string (`""`/`''`) → `valueAlreadyPresent` (it's a real value).
- Parent block is missing, is a list, or is written inline (`{…}`) → typed error, never a corrupt edit.
- A reference-using or malformed document → typed error.

## 2. Scope

**In scope:** `insert` (static); **fill** a null scalar leaf; **insert** a new leaf key into an existing **block** map (append at end, sibling indentation); reuse of the navigation, guards, value/error/serializer machinery; tests.

**Out of scope (typed errors / deferred owners):** **creating missing parent blocks** (`["a","b","c"]` where `a.b` is absent) — a later increment (nested multi-line insertion); **inserting into an inline `{…}` map** or **appending to a list** → `unsupportedValueShape` (different mechanics; deferred); **overwriting a real value** → `valueAlreadyPresent` (that is `set`'s job); **positional / sorted insertion** and **attaching a comment to the new key** (ruamel offers these; a later opt-in); **multi-document** input → first document only.

## 3. Design / approach

**Public surface** (added to the existing `YAMLEditor`):
```swift
extension YAMLEditor {
    /// Writes `value` at `path` when it isn't there yet: fills a null value, or
    /// adds a new key (appended at the end of its existing block, at the siblings'
    /// indentation). Add-only — throws `valueAlreadyPresent` if a real value is
    /// already there (use `set` to change it). Everything else stays byte-identical.
    public static func insert(_ yaml: String, at path: [YAMLPath.Component], to value: Value) throws -> String
}
```

**Pipeline** (all Swift over the source bytes; reuses `set`/`unset`'s parse + guarded navigation):
1. **Guarded parse; refuse anchor/alias** documents (the existing scan).
2. **Navigate `path`, validating each step.** Split on the last component:
   - **Full path resolves to a node** → it exists; classify:
     - **null node** → **fill** (step 3a). Detection is by the parser's **node kind** (D5): a value the parser labels *null* is fillable; `""`/`''` are labeled *scalar* (a real value) and fall to the next case. (A probe confirmed 0.6.2 labels the no-value spellings null and `""` scalar; the source-byte check the first draft described was dropped — see §7.)
     - **real scalar** → `valueAlreadyPresent`.
     - **map / sequence** → `notASingleValue`.
   - **Last component absent, but its parent resolves** → candidate to *insert* (step 3b). Parent must be a **block map**: an inline `{…}` map or a sequence/non-map parent → `unsupportedValueShape`; a parent that doesn't resolve at all → `pathNotFound` (no `mkdir -p`).
3. **Edit:**
   - **(3a) Fill.** The null's node mark is unreliable (for a bare `key:` it lands on the *next* line), so scan back from it to the key's line, find the key/value colon (`keyValueColon`, quote-aware), and rewrite the value slot: `<indent>key: <token>`, dropping whatever null spelling sat there and **keeping any same-line trailing comment plus the whitespace before it** (like `set`). `token = emitScalarToken(value)`.
   - **(3b) Insert.** The parent (block) map's mark points at its **first child key** → the children's indentation. Walk the block to the **dedent** (indentation drops below the children) and append `<indent><key>: <token>` after the block's last **data** line — before a trailing block-level comment/blank — matching the document's line ending. A flow (`{…}`) parent is caught by a byte check (`{` at the parent's mark). The **key** is emitted quoted if it needs quoting.
4. **Return** `String(decoding: splicedBytes, as: UTF8.self)`.

**New/notable helpers:** the key-line rebuild for fill (scan back to the key's line, preserve the trailing comment + its whitespace); the **dedent-scan block-end finder** (the core new mechanic); `keyValueColon` (quote-aware); a byte check for a flow parent (`{` at the parent's mark); the key/value `emitScalarToken` (quote-if-needed).

## 4. Tests (`Tests/YAMLTests/InsertTests.swift`)

- **Fill:** bare `a:`, `a: ~`, `a: null` → `a: <value>`; number vs string intent (`to: 1` plain, `to: "1"` quoted); oracle re-parse.
- **Insert new key:** append at end of a block, sibling indentation, siblings + comments byte-identical (Example 2.2-style commented fixture); nested block; a key that needs quoting is emitted quoted.
- **Refusals (typed error, never corruption):** real value present; empty string `""`/`''` present; inline `{…}` parent; list/sequence parent; missing parent (no `mkdir -p`); reference-using doc; malformed doc.
- **Preservation:** comments and blank lines around the edit unchanged; a filled value keeps a same-line trailing comment; a multi-line document touches only the target line.
- **Bump tripwires (`EngineNullContract`, a separate suite):** pin the engine behavior `insert` (and decoding) rely on — the no-value spellings label as *null*, `""`/`''` label as *scalar*, and a no-value decodes to `nil` (not `"null"`). A future yaml-cpp update that shifts any of these fails loudly and points at the cause ([#1290](https://github.com/jbeder/yaml-cpp/issues/1290)-class regressions).

## 5. Risks & mitigations

- **A future engine bump changing null handling.** *Not* the current engine — a probe confirms 0.6.2 labels no-value *null* and `""`/`''` *scalar*. The cited [yaml-cpp #1290](https://github.com/jbeder/yaml-cpp/issues/1290) is a **0.8.0** *read-null-as-text* bug on the **decode** path, not a classification bug here. *Mitigation:* **bump-tripwire tests** (`EngineNullContract`, §4) pin the classification (`a:`/`~`/`null` → null; `""`/`''` → scalar) and that a no-value decodes to `nil` (not `"null"`); a bump that shifts either fails loudly and names the cause.
- **Parent-map extent detection (the core new mechanic).** Finding "the last child's last line" for the append point is where correctness lives — nested blocks, a last child with a multi-line value, trailing comments/blank lines. *Mitigation:* spec-derived fixtures + the re-parse oracle; when in doubt, refuse rather than mis-place.
- **Indentation matching.** *Mitigation:* copy a sibling key's exact leading whitespace; never assume a 2-space step.
- **Inline `{…}` / list parents.** *Mitigation:* refuse (as `set`/`unset` refuse inline collections) — never a partial edit.
- **Key needing quotes.** *Mitigation:* emit the inserted key through the same needs-quoting logic as scalar values.

## 6. Placement & verification

- **Files:** `Sources/YAML/YAMLEditor.swift` (new `insert` + private helpers; new `YAMLEditError.valueAlreadyPresent(path:)`), `Tests/YAMLTests/InsertTests.swift`. No `Package.swift`/shim/vendored changes.
- **Three passes:** `swift test`; `swift test -c release`; `swift run -c release --package-path Projects/InteropRepro`.
- **On landing (done, 2026-07-17):** flipped the Phase 3 feature-4 entry to `IMPLEMENTED` in `Roadmap/phase-3-in-place-editing.md` with a change-log line; flipped the `Specs/README.md` 005 row to Implemented and this plan's Status to "merged to `main`". Phase 3's `ACTIVE → COMPLETE` call left pending for the maintainer.

## 7. Status log

- **2026-07-16 (planned).** Scope + design via the cold-reader Q&A. Unknowns → **Q1 = B** (fill-null + insert-leaf-into-existing-block), **Q2 = A** (a new named command), **Q3 = A** (append at end of block, sibling indent), **Q4 = A** (add-only; throw when a real value is present), **Q5 = A** (fill genuine nulls; refuse `""`; detect from source bytes). Grounding via web research: Swift API guidelines + the boolean-flag anti-pattern ([Swift.org](https://www.swift.org/documentation/api-design-guidelines/), [Fowler](https://martinfowler.com/bliki/FlagArgument.html)) and Foundation's add-a-named-method precedent ([`Dictionary.updateValue`](https://developer.apple.com/documentation/swift/dictionary/updatevalue(_:forkey:))); append-at-end from the comment-preserving editor default ([ruamel](https://ruyaml.readthedocs.io/en/latest/example.html)) and no-alphabetical-norm ([yamllint key-ordering off by default](https://github.com/adrienverge/yamllint/blob/master/yamllint/rules/key_ordering.py)); add-only from `Set.insert` vs [`Set.update(with:)`](https://developer.apple.com/documentation/swift/set/update(with:)-7r2g) and [least-astonishment / fail-loud](https://lawsofsoftwareengineering.com/laws/principle-of-least-astonishment/); the null-vs-empty-string distinction and the engine conflation risk ([yaml-cpp #1290](https://github.com/jbeder/yaml-cpp/issues/1290)). Surfaced that last item as a design constraint (source-level blank detection), not just a note.
- **2026-07-16 (plan review — 2 findings, resolved).** (1) **Changeset self-inconsistency (roadmap).** The staged set added this plan + the index row but left `Roadmap/phase-3-in-place-editing.md` contradicting it — three features listed, an Out-of-Scope bullet reading "inserting brand-new keys… out of scope," and a changelog concluding "ready to move ACTIVE → COMPLETE." Per the atomic-changeset principle (a commit must leave the tree coherent, not contradictory), updated the roadmap **in this same changeset**: added feature 4 (`insert`, PLANNED), narrowed the Out-of-Scope bullet to *creating missing parent blocks* (carving out the in-scope leaf-into-existing-block case), extended the phase success metric to "added/filled," and added a changelog entry that supersedes the "ready to COMPLETE" note (a fourth feature keeps the phase ACTIVE). This corrects an earlier defer-to-merge call — right for feature 004, whose Phase 6 home merely *omitted* it, but wrong here, where the Phase 3 home actively *contradicted* it. (2) **Taxonomy summary mismatch.** A4 read "incompatible parent → `pathNotFound`," but §3 (authoritative) splits it — a parent that doesn't resolve → `pathNotFound`, a resolved-but-unusable parent → `unsupportedValueShape`; A4 aligned to §3. Grounded in atomic-commit / self-consistent-changeset guidance.
- **2026-07-16 (pre-implementation Q&A + implemented).** Four mechanical unknowns resolved before coding: **name** = `insert(_:at:to:)` (mirrors `Set.insert`); **null detection** = trust the parser's node kind (probe-verified) + **bump-tripwire tests** (reviewer's request); **nested reach** = handle a block whose last entry is multi-line via the indentation dedent rule, placing the new key after the block's last data line, before any trailing comment/blank; **fill + comment** = keep a same-line trailing comment. **#1290 corrected** — it's a 0.8.0 *read-null-as-text* (decode) bug, not an empty-string-becomes-null classification bug; D5/§5 rewritten and the risk re-homed to a future decode-path bump, guarded by tripwires. **Four empirical probes (retired)** established the engine's mark behavior — a bare null's mark lands on the *next* line (so the fill scans back to the key's line), a block map's mark points at its first child key (giving the children's indentation), and a flow map's mark points at `{` (flow-parent detection). Implemented `YAMLEditor.insert` + the guarded navigation-to-parent + the unified null-fill + the dedent-scan append + `keyValueColon`; added `YAMLEditError.valueAlreadyPresent`. Tests: `InsertTests` (19) + `EngineNullContract` (3). Green: **145** tests, three passes (2 pre-existing known issues, unrelated). Sole product surface: `Sources/YAML/YAMLEditor.swift` + the error enum — no `Package.swift`/shim/vendored changes. Left uncommitted for review.
- **2026-07-16 (post-implementation review — 4 findings, resolved).** (1) **Dead `tilde` constant** — orphaned when the fill was unified to rebuild the key's line (no per-token branch); removed. (2) **Stale §3 prose** — the design step still described the source-byte null check that D5/§7 had replaced with node-kind detection; §3 rewritten to match the implementation (and the mechanics: bare-null-mark-on-next-line, dedent scan, flow-parent byte check). (3) **Fill collapsed multi-space-before-comment** (`a:    # note` → `a: x # note`) — inconsistent with `set`, which leaves post-value bytes byte-identical; **fixed** to preserve the whitespace run before a trailing comment (1-space cases unchanged, so all prior tests stayed green; a multi-space test added). Research noted ruamel normalizes whitespace, but internal consistency with `set` was decisive and the fix was free. (4) **Mixed line endings** — the append's terminator-less-last-line branch always emitted a lone `\n`; **fixed** to match the document's existing ending (CRLF if it uses CRLF), with a test. Grounded in dead-code hygiene, docs-as-code/SSOT, and set-consistent byte preservation. Green: **147** tests, three passes.
- **2026-07-17 (status flip).** Feature landed on `main` as PR #7 but its status markers were never flipped (an overdue debt, caught in review). Flipped in a dedicated status commit: `Specs/README.md` 005 → Implemented, this plan's Status → "merged to `main`", and `Roadmap/phase-3-in-place-editing.md` feature 4 → `IMPLEMENTED` with a change-log line — batched with the same-overdue 004 flip.
